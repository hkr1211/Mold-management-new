# 复制为 .env 后按需修改（不要把 .env 提交到 git）
#
# SQLite 数据库文件路径：
# - 本地直接运行（非 Docker）时可用相对路径，例如：./data/mold_management.db
# - Docker Compose 默认在容器内使用：/app/data/mold_management.db（见 docker-compose.yml）
SQLITE_DB_PATH=/app/data/mold_management.db

